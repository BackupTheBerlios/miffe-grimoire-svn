inherit  perl--modules
   VERSION=( "0.53" "0.51" "0.50" )
    SOURCE="A/AU/AUTRIJUS/Module-Signature-$VERSION.tar.gz"
  HOMEPAGE="$HOMEPAGE/search?dist=Module-Signature"
  ESTIMATE="10"
      DESC="$SPELL, a module to check and create SIGNATURE files
for CPAN distributions" 

build() {
  true  |  perl_module_build
}
