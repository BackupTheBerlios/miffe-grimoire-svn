inherit  perl--modules
   VERSION=( "2.34" )
    SOURCE="M/MS/MSERGEANT/${SPELL#perl--}-$VERSION.tar.gz"
  HOMEPAGE="$HOMEPAGE/search?dist=XML::Parser"
       REQ="expat perl"
  ESTIMATE="54"
  EPROVIDE="XML-Parser"
      DESC="$SPELL provides perl module for parsing XML documents"
